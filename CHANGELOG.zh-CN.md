# 更新日志

本文件记录 MQ Studio 的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/spec/v2.0.0.html)。

[English](CHANGELOG.md)

## [未发布]

## [0.0.9] - 2026-09-06

ActiveMQ 是第八个驱动，也是这里第一个「一个名字，两个产品」的家族。Classic
5.x / 6.x 与 Artemis 2.x 除了名字之外底层没有一处相同，而连接建立时是哪一个应
答，决定了之后每一次调用怎么发。

同时，RocketMQ 的命名空间可以在侧边栏里直接选，不必再手工加在每个名字前面；而
连接在线时改动驱动设置现在会重新拨号，不再默不作声地继续用旧的。

### 新增

- Amazon SQS 是第十个驱动，也是第一个完全没有 Broker 地址的家族。这里没有任何
  东西可以拨号：一个连接就是一个 AWS 区域加一份凭据，端点由 SDK 从区域解析出
  来。驱动自己的连接表单不声明地址字段，这正是 profile 能在地址为空时保存下来
  的原因——这道接缝是在这个驱动之前就铺好的，而不是做到一半才加的。连接列表在
  其他家族显示地址的位置显示区域。凭据留空时会回落到本机已有的 AWS 身份，
  在 AWS 内部运行时就是这么用的。

  队列会把持有的消息拆成三类展示——可取、处理中、延迟中——因为它们对应三种不同
  的问题、三种不同的处理方式：没有消费者在取、消费者取走了却没处理完、以及队
  列正按要求工作。标准队列和 FIFO 队列都可以创建、编辑、清空和删除。发送支持
  命名属性、延迟、重复份数，以及 FIFO 队列必需的分组 ID 与去重 ID。还有死信，
  通过反向遍历每个队列的重驱策略找出来，并列出各自的来源队列——一个还有堆积、
  却已经没有来源的死信队列，既不会再收到新消息，也不会自己变空。

  浏览带一条注意事项，按下按钮之前值得先读。SQS 只有一种读取方式——
  ReceiveMessage，也就是消费者用的那一个：它返回的消息在应用放回去之前对其他
  消费者不可见，而且每条消息的接收次数都会永久加一。如果队列配了重驱策略，
  反复浏览会把一条什么都没失败的消息推进死信队列。

  没有消费者页、没有集群页，也没有访问控制页，三者都不是遗漏。SQS 没有任何形
  式的订阅——消费者就是调用 ReceiveMessage 的那个人，服务不记录他是谁——所以队
  列的订阅者数上报为「未知」而不是 0。服务由 AWS 运行，所以没有节点、没有磁盘
  数据也没有速率：SQS 给出的都是单个队列的数字，其余在 CloudWatch。而谁能调用
  什么由 IAM 决定，那是更外面一层服务。

- NSQ 是第九个驱动，也是第一个完全没有管理协议的家族：运维需要问的一切，都是
  对承载消息的同一批守护进程发起的 HTTP 调用，所以它不需要任何线协议客户端。
  一个连接是一组 nsqd 地址而不是单个端点，因为主题只存在于创建它的那个守护进
  程上，应用展示的每一个数字都是这组地址的求和。

  主题及其堆积量，并区分滞留在主题自身队列与滞留在通道里的部分；通道，也就是
  这个家族的消费组，带堆积、投递中、延迟待发与重新入队的计数；创建、清空、暂
  停和删除两者，一次覆盖所有守护进程；向指定的某个守护进程发送，可重复发送或
  延迟投递；集群里的 nsqd 与告诉消费者去哪里找它们的 nsqlookupd 并列展示，两
  边对地址不一致时给出告警；以及已连接的客户端，涵盖 nsqd 上报它们的两种角色
  —— 消费者带 ready 计数，指出谁已经不再请求消息；生产者带各自已发送的条数。
  只有持有连接的生产者才会出现：通过 HTTP 发送是一次请求而不是一条连接，nsqd
  应答完就没有东西可列了。

  没有消息板，也没有死信，两者都源自同一个事实：nsqd 把消息交给消费者之后就不
  再持有它。堆积数背后没有可回读的日志，没有任何东西为消息建索引，重试超过上
  限的消息是被丢弃而不是被挪到别处。也没有位点，所以通道的堆积只能被消费或被
  清空，没有第三种办法。

  有两件守护进程的行为是必须告诉应用的。删除必须同时作用于 nsqlookupd：守护进
  程会忘记被删除的主题，服务发现层不会，所以只删到 nsqd 的操作会把名字留在那
  里，消费者查询时依然能找到它。清空主题时必须一并清空它的通道：nsqd 在消息到
  达时就把它复制进每个通道，所以只清空主题会返回 200，而任何人能看到的数字都
  纹丝不动。

- ActiveMQ 是第八个驱动，一种连接类型覆盖两个产品：Classic 5.x / 6.x 与
  Artemis 2.x 在连接建立时按哪个 MBean 域应答自动区分。它们底层没有一处相同
  —— Jolokia 路径不同、ObjectName 的键不同、属性名不同，连浏览结果的键都毫无
  交集 —— 而这些都不会漏到页面上。

  队列与主题在同一块板上，含堆积、计数器与配置；两种产品上的持久订阅，可建
  可删；浏览不会取走任何消息，因为在两个产品上它都是管理操作；带 JMS 消息头、
  属性和优先级的发送；沿声明反向查出死信目的地，并把消息重投回它们当初失败的
  地方；broker 的存储、journal 与生效配置，以及它桥接到的其他 broker；客户端
  连接及各自使用的协议，并可断开。当 broker 的 AMQP 接入点可达时，还可以实时
  监听一个主题 —— 这是管理面唯一做不到的事，因为 JMX 没有推送。

  有两处缺失属于 broker 而不是本应用。没有延迟投递：两个产品其实都有，但它们的
  发送操作都只接受字符串 map，而调度标注必须是 Long，所以设了延迟会被接受然后
  被静默忽略。以及 Classic 的浏览无论目的地多深都止步于 400 条，这一点页面会
  明确报出而不是藏起来。

- RocketMQ 的命名空间现在可以在侧边栏里直接切换。它放在页面导航的上方，因为它管着
  所有这些页面 —— 命名空间是客户端给每一个 topic 和消费组名字加的前缀，改掉它等于把
  整个标签页重新指了一个地方 —— 选中之后会存回这个连接并重新拨号，所以下次打开还停
  在你离开时的那个命名空间。列表是从集群自己的名字里读出来的，因为 RocketMQ 根本不
  存命名空间这种对象；还没有任何东西用过的命名空间，直接输进去就行。(#78)

### 修复

- 连接在线时改它的驱动设置 —— RocketMQ 的命名空间、Kafka 的安全协议 —— 改动会存下
  来，但读取仍然按旧的走，一直到重启应用为止，而界面上没有任何地方说过这件事。此前
  只有地址、超时和凭据的变化会触发重连。

## [0.0.8] - 2026-09-05

只有一处改动，关于发布产物存放在哪。快速镜像所在的存储桶不止服务这一个项目，而
之前一次发布是直接写在它的根目录下的 —— 两个项目发布同一个 tag 就会撞在一起。

### 变更

- 快速镜像上的安装包从 `dl.amigoer.com/<tag>/` 移到了
  `dl.amigoer.com/mq-studio/<tag>/`。GitHub 镜像没有变化，已发布的摘要也全部没有
  变化：改变的是文件从哪里供应，而不是文件是什么。
- 0.0.7 的安装会通过 GitHub 而不是快速镜像拿到这个版本。它构建时带的路径指向旧位
  置，而清单里说什么都无权重定义一个构建自带的镜像 —— 正是这条规则挡住了镜像去改
  写一个已安装应用的下载地址，代价则是要用一个版本来完成迁移。从 0.0.8 起两个镜像
  重新都能用。

## [0.0.7] - 2026-09-05

这个版本讲的是更新怎么送到你手上。此前一切都走 github.com —— 检查、下载，以及官网
自己的按钮 —— 所以一个连不上它的网络，既不会知道有新版本，也装不上。现在一次发布
由一份很小的清单描述，多个主机都提供它，而具体由哪个主机作答，是在你自己的机器上
量出来的，不是提前替你定好的。

Windows 的安装器则是压根没跑起来过。点安装会报权限提升失败，因为应用启动它用的那个
调用根本无法弹出授权提示。这个修复救不了它自己：如果你在 Windows 上用的是 0.0.6
或更早的版本，这一次请手动下载安装。

### 新增

- 一次发布现在有多个镜像。它由一份每个镜像都提供的清单描述，每个安装包的摘要都写
  在里面；应用会让这些镜像竞速去取这份清单 —— 反正检查更新本来就要取它，于是这次
  获取顺带就量出了当前网络能连上哪个主机。胜出的那个会被记住一周，所以一次例行检查
  只有一个请求，根本不发生竞速。如果某个镜像在下载中途不再响应，下一个会接着下 ——
  这之所以安全，是因为摘要写在清单里而不是放在安装包旁边：镜像只负责搬运字节，无权
  声称这些字节是什么。(#62)
- 清单本身可以声明镜像，这些会被并入构建时自带的那份列表，所以将来新加的镜像也能
  触达在它出现之前就发布出去的版本。合并只增不改 —— 镜像说什么都无法删除或重定义
  已经编译进应用的那些路径。
- 官网的下载按钮现在同时带一个镜像链接和一个备用链接，两者都在构建页面时解析好。
  官网不发任何 JavaScript，量不出访客能连上哪一个；同时给出两个，是它能诚实做到的
  上限。

### 变更

- 检查更新读的是那份清单，不再是 GitHub 的 releases API；官网生成下载卡片时读的也是
  同一份。两者不再可能对「最新版本是什么」给出不同答案。

### 修复

- 在 Windows 上安装更新会失败并提示「请求的操作需要提升」，且从不弹出授权框。启动
  安装器用的那个调用不读程序的 manifest，也无法提权，所以面对一个要求管理员权限的
  安装器，它既不提示也不运行。(#77)
- 更新失败时显示的是运行时自己的错误文本，前面还带着类名，于是「RuntimeError:
  failed to start the installer: fork/exec C:\Users\...」原样送到了读者眼前。
- 含义固定的失败现在会用你的语言表述：授权提示被取消、当前安装方式无法自行替换、
  当前版本太旧读不懂发布索引。而细节本身就是重点的那些失败 —— 哪个镜像超时、返回了
  什么 —— 仍然按原样显示。
- 「关于」页面现在列全了七个驱动。此前只有六个，漏了 NATS，而且把 Redis Stream 写成
  「Redis」—— 这是全仓库唯一一处被第七个驱动落下的家族列表。

## [0.0.6] - 2026-09-03

NATS 是第七个驱动，也是这里第一个答案来自四处的家族 —— 协议本身、JetStream、
服务端的 HTTP 监控端点，以及系统账户。连接建立时四层全都会探一遍，每个页面也都
会说明自己读的是哪一层，或者为什么是空的。

同时修掉了三个只有真实 broker 才照得出来的凭据问题：拨号时连接存下来的认证方式
被重置成了「无」、RocketMQ 的全局 AccessKey 被盖到别的家族的连接上、以及
RocketMQ 自己的那对密钥其实从来没有被用来签过名。

### 新增

- NATS 是第七个驱动，走官方 Go 客户端及其 jetstream 子包。JetStream 的流及其
  主题、留存策略、存储方式与副本集；push 与 pull 两种消费者，带待处理、未确认与
  重投计数；按 sequence 浏览与实时跟随；按 subject 发布，并支持等待回复的
  request；按条数、sequence 或 subject 清理，以及删除单条消息；集群里的服务器及
  其路由与生效配置；客户端连接及各自订阅了什么，并可以断开其中一条；还有账户页，
  显示各账户的 JetStream 用量与它们被给到的上限。
- 为核心 NATS 准备的订阅台 —— 这是唯一放不进既有页面的部分。一个 subject 什么都
  不留：消息发给此刻在听的人，之后就没了，没有历史可以往回翻，所以它是实时视图，
  而不是加了过滤条件的消息浏览页。
- NATS 连接建立时会探测四层 —— 协议、JetStream、服务端的 HTTP 监控端点、以及系统
  账户 —— 后三层每一层都有两种不同的缺席原因，指向不同的地方。服务端没编译进
  JetStream，和账户没被授权用它，不是一回事；没人填过监控地址，和填了但打不通，
  也不是一回事；没给凭据和给了被拒，同样不是。所以上报的是六条原因而不是一条，
  因为每一条把人送去的位置都不同。
- 集群相关页面用监控端点和系统账户里应答的那一个。监控端点只答表单上填的那一台
  服务器，`$SYS` 才会扇出到集群里的每一台，所以每一行都记录了自己的来源，页面也会
  说明某个数字只覆盖了几台里的一台。
- 三条 NATS 专有、别的家族都没有的告警规则：流的 Raft 组没有 leader、流的副本落后、
  以及服务器因为客户端跟不上而把它踢掉。
- 两种新的认证方式：nkey seed，以及 operator 签发的 creds 凭据文件。
- RocketMQ 连接可以填写命名空间，填了之后这个连接做的每一件事都被限定在该命名空间
  内：Topic 和消费组按短名列出，而连接发出的每一个请求带的都是拼接后的全名。字段在
  连接表单的「高级」区里，那里原本就为它画了一个禁用的「实例 ID」控件；命名空间会
  显示在连接列表和标签状态栏的地址旁边，这样才分得清一个连接是不是被限定过。留空
  即维持原来的行为：连接看到的是整个集群，包括带前缀的原始名字。(#61, #63)

  这是 RocketMQ 5.x 真正实现的那一套命名空间 —— 客户端侧的那套：`orders` 到线上是
  `ns%orders`，消费组的重试 Topic 是 `%RETRY%ns%GID`。Broker 存的就是一个普通
  Topic，对命名空间一无所知，所以它在未做任何 broker 配置的原生集群上就能用。它不是
  `namespaceV2`：那一套发的是两个请求头字段，而 apache/rocketmq 里没有任何代码读它们
  ——这边做了也不会被兑现，也没有任何环境能证明它生效。

### 修复

- 现在拨号用的是这条连接存下来的那个认证方式。此前把 profile 解析成待拨号形态时，
  会把除 RocketMQ 之外所有家族的认证方式重置成「无」，于是一条存着用户名密码的连接
  是以匿名身份去连的。这一直没被发现，是因为在此之前有认证方式的驱动都直接从存下来
  的凭据里取值，压根不看这个字段。
- 设置里 RocketMQ 的全局 AccessKey 不再被盖到别的家族的连接上。此前它会替任何没有
  自带凭据的 profile 兜底，结果是 NATS 或 Kafka 的连接被用一个它的驱动根本没实现的
  认证方式、拿着另一个中间件的凭据去拨号 —— 而且只有配过那对全局凭据的用户会遇到。

- 连接测试失败会说明原因了。原因一直是算出来、翻译好，然后写进 `title` 属性的，
  而 macOS 的 webview 不画原生提示框，于是这个按钮唯一要产出的东西没人看得到，
  失败了只说一句连不上。现在它就写在按钮旁边。并排的两个字段也不会再错位 ——
  此前一条换行的提示只会把自己那个输入框往下推，旁边那个还留在顶上 —— NATS 的
  那几条提示原本是别的家族的三倍长，现在每条都只有一句。

- RocketMQ 连接的 AccessKey 与 SecretKey 现在真的会发出去。它们一直被保存、解密
  并交给驱动，而客户端库两个都没读 —— 它根本没有签名代码 —— 所以每一次管理调用
  都是以匿名身份到达的。在 `aclEnable=true` 的集群上，这只在 broker 的全局白名单
  恰好覆盖了调用方时才行得通，而连接表单上的两个凭证字段、以及设置里那对为其兜底
  的全局凭证，都在做一个没人兑现的承诺。现在请求会带上 access key，以及对排序后的
  头部字段与消息体计算的 HMAC-SHA1 签名 —— 这正是 broker 那侧重新拼出来做比对的
  内容。

- 更新弹窗里的出口指向站点，不再是 GitHub Releases。检查失败、安装失败、以及
  无法替换自身这三个按钮原本都打开发布页，而对连不上 github.com 的人来说那是条
  死路；现在它们打开站点的下载区，弹窗里那条常驻链接也改成打开站点上的更新日志，
  两者都跟随应用当前的语言。检查和下载本身仍然走 GitHub，所以这次改的只是按钮
  落到哪里。(#62)

- 每次启动都会检查新版本了：窗口起来五秒后就查，而不是把上次检查之后剩下的那段
  二十四小时间隔等完。一个一天开关几次的应用永远走不到间隔的尽头，于是后台检查
  一次也没跑过，想知道有没有新版本只能自己去设置里点按钮。一直开着的会话，间隔
  不变。(#67)

- 跳过一个版本不再让「检查更新」失效。「跳过此版本」的意思是别再提醒，所以用户
  自己点的检查现在会把这个版本从跳过列表里取回来并重新给出：以前它的回答是「已是
  最新版本」，报的还是正在运行的那个版本号，而且跳过之后没有任何地方能撤销。设置
  里的卡片也会直接说这个版本被跳过了，而不是一边报着「发现新版本」，一边把按钮换
  成「检查更新」。(#66)

- 更新弹窗底部两个角不再是直角。底栏是唯一有不透明背景的那一条，而只靠弹窗自身的
  圆角裁剪在 WebKit 上不够用 —— 一个带变换、带动画、子树还会滚动的盒子，它会把这个
  裁剪丢掉，而这三条正好是弹窗自己的全部特征。现在底栏自己带上了圆角。(#53)

## [0.0.5] - 2026-09-02

一次接入三个驱动 —— Redis Stream、Pulsar 与 MQTT，可连接的家族增加到六个。每个
驱动都按各自家族本来的说法来写，而不是把别的家族的概念翻译过来：别的家族有死信
队列的地方，Redis 有的是待处理消息列表；RocketMQ 用 Tag 的地方，Pulsar 用的是
属性；而 MQTT 根本没有管理面，一个连接能做什么是拨号时探出来的，不是事先声明的。

### 新增

- Redis Stream 成为第四个驱动。Stream 的长度、内存占用与消息 ID 区间；消费组的堆积量，
  以及 XGROUP SETID 支持的全部重置位置方式；按时间窗口或 ID 浏览消息，并按其本来的
  有序字段形式写入；待处理消息列表（PEL）及认领、自动认领与确认；服务器的内存、持久化
  与慢日志；客户端连接；以及带键、频道和命令规则的 ACL 用户。单机、哨兵、集群三种部署
  都能连接，集群下的 Stream 列表会遍历每一个主节点，而不是只问所连的那一个。

- 有两个其它驱动都会给出的指标在这里被刻意留空，因为 Redis 本来就没有。它统计的是命令
  而不是消息，所以只有以「命令/秒」为名的速率，没有消息速率；它报告的是内存而不是磁盘，
  所以只有内存水位，没有磁盘百分比。没有设置 maxmemory 的服务器干脆不画水位条——没有上限，
  也就没有可以计算百分比的分母。

- 对这个家族来说，待处理消息列表取代了死信页。Redis 不会把消息挪到别处，也不会放弃投递：
  派发给某个消费者的消息仍留在 Stream 中，并一直归属于它，直到被确认或被别人认领走。
  「确认」被当作破坏性操作来二次确认，因为它确实是——它会在没有任何人真正处理过的情况下
  把消息了结掉。自动认领除了报告转移了哪些，还会报告哪些已经不在 Stream 中：这是唯一一个
  会明确告诉你「这部分工作丢了、而不是转走了」的地方。

- 接入 Apache Pulsar，第五个驱动。主题及其分区与存储方式、其上的命名空间与租户、带积压和
  游标的订阅、消息浏览与实时跟随、发送控制台、Broker、死信主题、角色授权和告警。数据面走
  二进制协议，其余走管理 REST API，两者分别上报：web service 能应答而 broker 端口不通的
  连接仍然可以读取所有页面，并会说明为什么发不了消息。
- 页面按 Pulsar 自己的语义写，而不是把别的中间件的说法翻译过来。这里没有 tag —— RocketMQ
  的生产者放进 tag 的东西，Pulsar 的生产者放进 property，所以发送控制台收集的是 property，
  消息页也按它筛选。订阅是 Broker 保存的游标，没有消费者连着也照样存在，所以「空闲」是正常
  状态而不是消费组消失了。Token 页列的是角色授权而不是账号，因为 Pulsar 授权的是 Token 的
  subject，集群本身不保存任何账号目录。
- 新增「订阅被阻塞」告警，这是其它中间件没有的。未确认消息超过上限后 Broker 会完全停止向该
  订阅投递 —— 只看积压的话和消费者变慢一模一样，但要靠确认消息或调大上限来解决，而不是去动
  消费者。
- **MQTT 3.1.1 与 5.0。** 这是本项目接入的第一个自身没有管理面的家族，因此一条连接
  能做什么是在拨号时决定的，而不是事先声明的，分三层：协议本身、多数 broker 会发布的
  `$SYS` 树，以及 EMQX 等自带的 REST 管理 API。探测不到的那一层会连同原因一起上报，
  页面会说明自己为什么是空的，而不是看起来坏掉了——而且两种情况是分开报的，因为
  「broker 拒绝了这个订阅」和「broker 根本不发布这棵树」要去不同的地方修。
- 带 QoS、retain 标志与 MQTT 5.0 属性的发布。结果会说明这是哪一种成功：QoS 0 没有
  任何确认，QoS 1 与 2 由 broker 应答，而 5.0 还能应答「收下了，但没有人可以投递」。
- 实时订阅工作台。它会报告丢弃了多少条消息、以及会话是否还在，因为悄悄丢消息的流和
  真的没消息的流看起来完全一样，断掉的连接和安静的 broker 也是。
- 由 broker 的保留消息得出的主题列表——那是 MQTT 唯一能枚举的东西。页面会写明这一点，
  这样不带 retain 发布的设备就不会被读成「这个设备不再发消息了」。
- 在 broker 提供管理 API 时，可以看到客户端与会话、它们的订阅、集群节点，并断开某个
  会话。比连接活得更久的会话会被标出来：broker 仍在为一个并不在场的客户端排队，而这
  在设备那一侧是看不见的。
- MQTT 自己的告警规则。此前它落到了 RocketMQ 的规则上，那些规则读的是 broker 序号、
  commitlog 磁盘占用和死信主题——这三样 MQTT 都不上报——所以 MQTT 连接根本无法触发
  任何告警。

### 修复

- 家族名带连字符的驱动，其降级原因会以原始 key 而不是完整句子渲染。在此之前没有任何家族
  的名字里带连字符。
- 任何家族的降级原因此前都看不到。承载它的侧边栏条目是 disabled 的，根本收不到指针
  事件；原因本身写在 `title` 属性里，而 macOS 的 webview 压根不渲染这个属性；再加上
  没有自己那套原因文案的家族，显示出来的只会是键名本身。

### 说明

- Paho 的两个 Go 库互不重叠：一个只说 MQTT 3.1.1，另一个只说 5.0，而配置成其中一种的
  broker 会拒绝另一种的 CONNECT。两个都用上了，协议版本是连接表单上的一个字段，而不是
  协商出来的。
- TCP、TLS、WebSocket 与 WebSocket over TLS 都支持。

### 已知限制

- macOS 版本尚未使用 Apple 开发者证书签名，磁盘映像内附「首次运行」助手，用于
  清除隔离标记。

## [0.0.4] - 2026-09-01

更新流程的完整闭环。新版本现在每次启动都会提示一次，直到被回应为止；而提示它的
那条通知本身，就是安装它的地方。

### 新增

- 有待处理的新版本时，每次启动提示一次；提示会一直留到被回应，而不是几秒后自己
  消失。这份记忆属于本次会话，所以被关掉而没有回应的版本，下次启动还会回来 ——
  「跳过」是唯一让它彻底停下的开关。用户自己按下的检查则直接打开对话框，因为他
  本来就在等这个答案。
- 更新对话框承载了更新的全部 —— 更新内容、下载与进度、以及重启 —— 并且可以从
  标题栏和通知两处打开。安装不再需要专门跑一趟设置页，那里此前是唯一能装的地方。
  发布页在每个阶段都保留入口，因为应用自己换不掉自己的时候，那是唯一的出路。
- 发布说明按 Markdown 渲染，而不是把标记本身显示出来：加粗、链接、分隔线、GitHub
  的 `> [!IMPORTANT]` 提示块，以及跨源码行折行的列表项现在是一条而不是好几条。
  链接交给系统浏览器打开，因为 webview 没有回来的路。渲染器只产出 React 节点、
  不产出 HTML，遇到不认识的语法降级成普通段落 —— 发布说明是远端内容。

### 变更

- 标题栏的更新按钮从「刷新」图标换成向上箭头，并且指向更新本身：有待处理的版本时
  点开对话框、并在提示里写明版本号；没有时仍然是发起一次检查。
- 应用不再自称 RocketMQ 客户端。macOS 包、Windows 文件描述、Linux 桌面项与打包
  元数据、以及窗口描述统一改为「消息队列」，不会随着驱动增加而过期；broker 的名字
  归到 Linux keywords 里，并补上了 Kafka。

### 修复

- 标题栏图标比写下的尺寸大了 22%。它们用 `rem` 写、基准按 13 算，但缩放梯度缩的
  是整个文档而不是根字号，实际生效的是浏览器的 16。现在改用 px，回到 40px 的栏
  当初就是按 28px 设计的那个值。
- 搜索按钮比旁边所有元素都高，标签也比标签页大一号 —— 它用的是 shadcn 的整页默认
  尺寸，而这里不是一整页。
- 命令面板此前固定距顶 96px，在至少 750px 高的窗口里显得偏上。现在居中，代价是
  结果被过滤时输入框会跟着面板高度移动。
- 更新通知在被回应之后仍然留着：跳过一个版本，通知还在说它等着；点了下载，同一条
  通知又从头坐到尾。
- 中文句子如果正好在强调标记之后折行，句中会多出一个空格 —— 拼接时比较的是标记
  本身，而不是读者真正看到的那个字。

### 已知限制

- Pulsar、Redis Stream 与 MQTT 仍在界面上呈现但禁用。
- macOS 版本尚未使用 Apple 开发者证书签名，磁盘映像内附「首次运行」助手，用于
  清除隔离标记。

## [0.0.3] - 2026-08-31

Kafka 支持，直接走 Kafka 自己的协议，而不是旁边另接一个管理 API。Topic、消费组、
消息与访问控制都已接入；Kafka 本身不报的数字一律留白，不做填充。

### 新增

**Kafka 3.x 与 4.x**

- 通过 Kafka 自身协议连接集群，支持 SASL/PLAIN、SASL/SCRAM 与 TLS。SCRAM 摘要
  单独作为一个字段：SHA-256 与 SHA-512 在 broker 上是两套独立凭据，同一个用户在
  另一种摘要下会认证失败，把这种失败报成「密码错误」是在撒谎。
- 概览、Topic、消费组、消息、发送台、集群、访问控制、配额与告警。
- Topic 展示分区、leader、同步副本与离线副本、副本因子与最小同步副本，行内展开
  可看完整配置文档。支持创建、修改与删除；修改只动明确给出的配置项，不会把用户
  从未见过的设置悄悄重置回默认值。
- 消费组展示分区级与成员级 lag、状态、协调者与分配策略。支持 Kafka 的全部五种
  位点重置 —— 最早、末端、指定时刻、指定 offset、相对偏移 —— 以及把一个组的位点
  复制给另一个组、删除消费组。
- 按最新窗口、offset 区间、时刻或 key 浏览日志，以及实时跟随末端。浏览不加入任何
  消费组、也不提交位点，因此可以安全地对着生产环境使用。
- 发送支持 key、header、指定分区与 acks 级别，并回显消息落在哪个分区的哪个 offset。
- Broker 展示生效配置与日志目录，包括是哪些分区占掉了空间。
- ACL 按主体分组展示，以及集群自己保存的 SCRAM 用户。集群没有配置 authorizer 时，
  两者都会带原因降级，而不是报错。
- 清空 Topic，即 Kafka 自己的截断：现有记录全部不可再读，而位点不回零 —— 停在 900
  的消费者仍在 900，只是已经追平了。
- 把分区迁到别的 broker、看着它迁、以及取消进行中的迁移。Kafka 的迁移没有完成事件 ——
  分区不再上报它就是迁完了 —— 所以页面去读集群，而不是自己宣布成功。旁边是优先副本选举，
  用来把 leader 放回副本列表本来指定的位置。
- 客户端配额：限制挂在「谁在调用」而不是「调用什么」上，可按用户、应用或地址设置，
  也包括没有配额点名时它们各自回落到的默认值。
- 集群页的事务：集群知道哪些事务生产者、每个顶住了什么、顶了多久，以及它是否已经超过了
  协调者承诺中止它的时限。生产者中途死掉导致管道停住，只有这一页看得出来 —— 这种集群
  的其他每一处都是健康的。
- 告警来自分区健康（副本不足、离线、无 leader 分开计数）与消费组 lag。

### 修复

- 清除连接凭据实际上没有清除。此前只按名字删掉了 RocketMQ 的 AccessKey/SecretKey，
  其他家族的密码在「清除」之后仍然留在存储里，下次连接用的还是表单已经声明删掉的
  那一份。这个问题自 RabbitMQ 上线起就存在。
- 连接地址为空时的提示写的是 RocketMQ 的 NameServer，让 Kafka 与 RabbitMQ 的用户去
  找一个自己表单里根本没有的字段。

### 说明

- 有三样东西 Kafka 不报，本次也不编。没有死信页：Kafka 没有 broker 侧死信队列，
  `.DLT` 后缀是 Spring Kafka 的约定而不是 Kafka 的。没有任何速率，因为管理协议里
  就没有。也没有磁盘百分比，因为 Kafka 只报分区占用的字节数，不报承载它的文件系统。

## [0.0.2] - 2026-08-31

支持 RabbitMQ。管理面是完整的，消息面走 AMQP 而不是管理接口的 publish 与 get：
发送会等 publisher confirm，浏览的行为和一个真正的消费者一致。

### 新增

**RabbitMQ 3.x 与 4.x**

- 连接 broker 的 HTTP 管理插件，同时建立 AMQP 数据面连接。该连接在 broker 上以
  `mq-studio: <连接名>` 标识自己，运维可以认出是哪个客户端。
- 概览、队列、Exchange 与 Binding、连接与信道、消息、死信、发送台、节点。
- 队列带完整 arguments：classic / quorum / stream、持久化、TTL、最大长度与
  overflow、死信交换机与路由键、单活消费者。可声明、清空、在队列间搬运、删除。
- Exchange 与 Binding：四种类型外加 alternate exchange；Binding 带路由键与
  arguments，含 headers 交换机的 `x-match`。
- 浏览走 AMQP 而不是管理接口，可按路由键或 header 过滤。队列被原样放回，页面也
  写明浏览的代价：读过的消息回队列时会带上 redelivered 标记。
- 带 confirm 的发送：目标交换机与路由键、mandatory、持久化、优先级、过期时间、
  headers、correlation id、reply-to、content type。没有任何绑定能路由的消息会被
  如实报告为不可路由，而不是假装成功。
- 死信从 `x-death` 头还原：来自哪个队列、为什么被拒、被拒了几次、发生在什么时候。
  可单条或批量重投回原队列或指定目标，也可以直接丢弃。
- 连接与信道，含协议、心跳、prefetch、未确认数与流控状态，并可带原因强制关闭。
- 节点，含内存分解、资源告警、网络分区、broker 自己的健康检查、特性开关，以及
  哪些已弃用特性正在被真正使用。
- 虚拟主机：增删改、默认队列类型、删除保护、tracing，以及连接数与队列数限流。
- 用户与权限：用户与 tag、按虚拟主机的 configure/write/read 正则三元组、主题
  权限、按用户的限流。改一个用户的 tag 不再需要知道他的密码。
- 策略与操作员策略，含优先级、pattern 与 definition，并显示某条队列实际命中了
  哪条策略；运行时参数与全局参数并列其中。
- 定义：导出整个 broker 或单个虚拟主机到文件；导入前先看清它会创建什么。
- Shovel 与 Federation：有哪些、是否在跑、没跑时 broker 自己给的那句话。只读与
  删除 —— 定义里带着对端 broker 的凭据，凭据在离开驱动之前就被抹掉了。
- stream 队列会报告通过流协议连上来的客户端，这些客户端从不出现在队列的 AMQP
  消费者列表里。
- 告警按 RabbitMQ 自己的指标推导：资源告警、网络分区、向水位线的逼近、队列堆积、
  无人消费的队列，以及被 broker 限流的连接。

### 修复

- 告警规则对所有连接都读 RocketMQ 的属性键，于是 RabbitMQ 被拿着它根本不上报的
  指标去衡量，无论 broker 有多糟都一条告警都不会触发。
- 管理面请求无视连接上配置的超时时间，因为底层库不接受 context。broker 一慢，
  页面就可以无限等下去。
- 密码错误被报成「请启用 management 插件」，把人支去重新配置一个本来没问题的
  broker。

### 已知限制

- Kafka、Pulsar、NATS、MQTT 等仍在界面上呈现但禁用。
- Shovel、Federation 与流协议是 RabbitMQ 的插件。没装这些插件的 broker 仍然保留
  对应页面，只是禁用并写明原因。
- macOS 版本尚未使用 Apple 开发者证书签名，磁盘映像内附「首次运行」助手，用于
  清除隔离标记。

## [0.0.1] - 2026-08-31

重构后 MQ Studio 的首个版本。MQ Studio 是一个消息中间件桌面客户端，围绕驱动端口
组织：对一种消息中间件的支持，是同一套接口背后的一个实现，而不是散落在每个页面里
的假设。

本版本仅支持 RocketMQ，其余协议在界面上呈现但禁用。

### 新增

**架构**

- 驱动端口：一种消息中间件就是一个驱动，实现同一套接口；其上的服务层与桥接层不关心
  回答请求的是哪一族。
- 能力模型：每个连接上报它所连的端点究竟能做什么，界面据此绘制。端点做不了的页面
  禁用并说明原因；这一族根本没有的概念不绘制。
- 多个连接可以同时打开，各自一个标签页和一套页面，每个请求都指明它跑在哪个连接上。

**RocketMQ 4.x 与 5.x**

- 总览、Topic、消费组、消息查询、死信、消息发送、集群健康，全部读取实时 NameServer。
- Topic 与消费组操作：Topic 的创建、编辑与删除；按时间重置消费组位点；把一个组的
  位点克隆给另一个组；直接写单个队列的位点。
- 消息操作：按 Key、Tag、时间窗或 msgId 查询；实时跟随一个 Topic；查看消息轨迹；
  重发死信；把一条消息投递给指定消费者并读回它的处理结果。
- 集群操作：读取 Broker 与 NameServer 的有效配置、查看副本落后多少、按需执行清理、
  摘除 Broker 写权限以便停机前排空。
- 访问控制：RocketMQ 5.3 的用户与授权可读可写；4.x 的 plain_acl 作为只写降级模式
  保留，并在页面上说明它读不回来。
- 告警：Broker 离线、消费组无实例、消息堆积、磁盘水位、死信，跨所有已打开的连接
  求值，在标题栏和独立页面呈现，可选桌面通知。

**支持平台**

- macOS（Apple Silicon 与 Intel）、Windows（x64 与 ARM64）、Linux（x64 与 ARM64，
  提供 .deb、.rpm 与 AppImage）。

### 已知限制

- 目前只有 RocketMQ 可以连接。RabbitMQ 驱动已经写好，但页面还没有建。
- 有四项操作被本应用所依赖的 RocketMQ 管理库的缺陷挡着，其中最明显的是消费组的创建
  与编辑。每一项都有一个断言当前行为的测试钉住，库修好即可解锁。
- macOS 版本尚未使用 Apple 开发者证书签名，磁盘映像内附「首次运行」助手，用于清除
  隔离标记。

### 说明

- 版本号从这里重新开始。0.1.x 那几个构建早于这次重构，已经删除。
